{
  "additionalProperties": false,
  "properties": {
    "athlete_id": {
      "description": "Target athlete; defaults to selected athlete in coach mode, or the only athlete otherwise. Format: i12345 or 12345.",
      "type": "string"
    },
    "item_id": {
      "description": "Required opaque upstream custom item ID to delete. This destructive operation has no confirm argument; the tool is registered only when ICUVISOR_DELETE_MODE=full.",
      "type": "string"
    }
  },
  "required": [
    "item_id"
  ],
  "type": "object"
}

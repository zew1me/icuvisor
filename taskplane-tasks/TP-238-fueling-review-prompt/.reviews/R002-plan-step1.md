# Plan Review — TP-238 Step 1

## Verdict: REVISE

The revised contract resolves the prior field-semantics, calculation, health-boundary, and Step 1/2 ownership gaps. Three route/verification details remain unspecified and can make a supposedly complete review silently incomplete.

1. **Make the prompt argument and date-resolution contract executable.** Name the exact arguments and their rules: whether `activity_id` is incompatible with `start_date`/`end_date` (or which wins), whether a supplied range requires both endpoints, and how the 1–90-day inclusive bound is enforced by the Step 2 handler rather than merely requested in rendered prose. Define the default exactly (for example, resolver offsets that produce 14 *completed* athlete-local days) and the behavior for invalid/reversed inputs. `race_name` alone also needs a bounded lookup policy or a request for `race_date`, because `get_events` requires `oldest` and `newest`; it must not trigger an unbounded calendar scan. When reading events, specify a limit and require `_meta.truncated` to make the result partial—not call an absent match “unconfirmed”—just as activity pagination makes an activity range partial.

2. **Use `include_unnamed: true` for the date-range activity index, or explicitly limit all coverage claims to named activities.** `get_activities` defaults to dropping empty-name rows after its bounded pagination. The current plan requires all pages and reports eligible/total sessions, but does not set this argument. Without it, unnamed workouts can be silently excluded from the denominator, missing-log count, and range g/h coverage. The contract and portable pack should require `include_unnamed: true` for an all-session fueling review, retain terse/full restrictions, and count unavailable rows separately.

3. **Turn the claimed Step 1 contract test into a concrete artifact.** The status now assigns a “contract-focused test” to Step 1 but does not identify its file or assertions, while the task’s Step 1 artifacts still omit it. Add (or explicitly extend) `internal/prompts/fueling_review_test.go` or `catalog_test.go` in Step 1, state that `FuelingReview` is its test name, and test the required rendered contract clauses: exact tools/arguments, `include_unnamed`, pagination and event-truncation behavior, field distinctions, positive-moving-time formula/coverage exclusions, and read-only/health boundaries. Keep the deterministic golden fixture in Step 2 as stated, then the existing targeted command will exercise a real Step 1 change.

# Plan Review — TP-238 Step 3

## Verdict: REVISE

The Step 3 checklist names the required surfaces, but it is not yet specific enough to preserve the evidence and safety contract established in Steps 1–2. Please amend the plan with these executable details:

1. **Replace the existing unsafe nutrition-library copy and its matching eval.** `web/content/cookbook/prompt-library.md` currently asks the assistant to “flag underfueled sessions” and to flag daily intake “low for the work done”; `CB-LIB-02` repeats the former. Those conclusions require an unstated individual target and the latter also invites mixing daily wellness intake with session load. Explicitly replace or remove both examples and update/remove `CB-LIB-02`. The replacement must request only logged `carbs_ingested_g` and an eligible `moving_time_seconds` g/h calculation, label absent logs as missing (not zero or inadequate), keep `carbs_used_g` and wellness daily macros separate, and prohibit targets/deficit/underfueling labels.

2. **Specify the new cookbook recipe and examples as a copyable implementation of the registered contract.** Name the page recipe `fueling-review`, add its card/link to `_index.md`, and make its two workflows exact: (a) a bounded recent-session/date-range review using the profile-first, `include_unnamed:true`, paginated activity route and the eligible-only g/h formula/coverage counts; and (b) optional same-day `race_date` calendar context that is sourced separately, does not scan by name alone, and supplies only conditional general educational material. The recipe, prompt-library snippet, and existing portable pack must all say facts are source-labelled and separate from general guidance, never recommend carb/calorie/sodium/fluid/sweat targets, prescribe nutrition, invent products, or call writes/streams/`include_full`. The portable pack was already created and linked in Step 1, so this step should preserve/only deliberately revise that single source rather than create a competing pack.

3. **Define the eval records, not just their topics.** Add concrete `CB-FUEL-*` scenarios with `recipe: "fueling-review"`, valid IDs, expected read tools, and an exhaustive read-only forbidden-tool list (including activity/wellness/event writes and `get_activity_streams`). Their `must_address` and `anti_patterns` must make the judge check: (a) g/h only when non-negative logged intake and positive `moving_time_seconds` are returned, with units and coverage/exclusions; (b) an absent intake is reported as a missing log, not zero/underfueling/estimated; (c) `carbs_used_g` is an upstream used/burned estimate distinct from ingested intake and daily wellness macros; and (d) a request for sodium/calorie/carbohydrate targets refuses individualized targets and offers only separately labelled general education/referral. Include athlete-local date resolution, source labels, and no-write/no-raw-payload requirements in the prompts. `--validate` checks only schema, tool names, and page existence, so those semantic assertions must be encoded in the scenario text/criteria.

4. **Make each required reference update concrete.** Add the `fueling_review` row with its five arguments and constrained, read-only workflow to `resources-prompts.md`; update its prompt-guardrail prose; change the cookbook and PRD “eleven curated prompts” statements and lists to twelve including `fueling_review`; add the cookbook index card; and add an `[Unreleased]` changelog entry for the MCP prompt, portable pack, cookbook recipe, and eval coverage. Keep all names/links consistent with the registered `fueling_review` prompt and the `fueling-review.md` recipe.

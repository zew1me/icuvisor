# Plan Review — Step 2

## Verdict: REVISE

1. **The bounded argument contract is not planned.** “Bounded planning/lookback/race context arguments” does not state defaults, allowed ranges, strict athlete-local date format, start/end ordering and maximum window, or whether `race_name` requires `race_date`. The Step 1 prompt is currently a static handler whose six arguments accept arbitrary strings, so registration alone will ship the opposite of the stated Step 2 requirement. Amend the step to define each default and bound, implement a validating handler (with short `UserError`s), and add table-driven tests for valid/default rendering plus non-integer/zero/out-of-range lookbacks, malformed/reversed/overlong planned dates, and a name-only race. The golden must use the normalized bounded scope.

2. **The registration test surface is incomplete.** Registering the prompt changes both hard-coded catalog contracts: `internal/prompts/catalog_test.go` currently expects twelve prompts, and `internal/mcp/protocol_test.go` also asserts a 12-item, alphabetically sorted `prompts/list` result. The latter is outside the listed file scope, so the described work will fail the package suite. Explicitly add `MastersPlanReviewPrompt()` to `NewRegistry()` in the intended ordering; update both counts/name lists (including the protocol test); add the new full golden case in `TestRenderedPromptsGolden`; and extend the existing committed `masters_plan_review_test.go` rather than creating a second focused test. Since the portable pack already exists and claims a registry prompt, also add it to `TestClientPromptPacksStayLinkedToRegistry` so its tool route cannot drift from the registered prompt.

# Plan Review — Step 1

## Verdict: REVISE

1. **The new bullets still do not define the required review sequence.** They establish what evidence is permitted, but not the ordered, athlete-local route that makes the evidence comparable. Add the sequence to the catalog/pack plan: establish timezone and resolve relative dates; partition non-overlapping baseline/history, current/completed, planned, and race windows; obtain event/plan/activity evidence (and label incomplete pagination/truncation as partial); then run the permitted baseline, spacing, ramp, and wellness checks. It must also distinguish a confirmed calendar race from a user-supplied race-date scenario anchor and retain current-day wellness as partial. Without this, the remaining “Review evidence sequence defined” checkbox can be satisfied while mixing windows or treating an unconfirmed race as observed evidence.

2. **The boundary test is still not scheduled as an executable Step 1 artifact.** `R002-4` says to add an artifact, but names no file, while Step 2 continues to defer `internal/prompts/masters_plan_review_test.go`; the Step 1 `go test ./internal/prompts -run 'MastersPlanReview'` command therefore still has no explicitly planned matching test. Amend Step 1’s artifacts/checklist to create that focused table-driven test (the prompt can be defined in `catalog.go` before it is registered in Step 2), and state that it asserts the separated sections/provenance, absolute no-write rule, no age/medical/score claims, hard-session and projection fallbacks, and every listed insufficient-evidence matrix case. Then the required targeted command actually verifies this step.

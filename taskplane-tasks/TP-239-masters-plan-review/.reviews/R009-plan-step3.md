# Plan Review — Step 3

## Verdict: REVISE

The checklist identifies the right broad surfaces, but it is not yet detailed enough to publish or evaluate the evidence contract approved in Steps 1–2.

1. **Publish the existing pack as one canonical, copyable contract.** `docs/prompts/client-prompt-packs/masters-plan-review.md` already exists and is linked to the registry; Step 3 should revise it only deliberately and keep it in lockstep with the cookbook rather than “add” a second pack. Specify a `masters-plan-review` cookbook page with valid front matter and an index card, both MCP-prompt and pasted-pack entry points, the bounded/default athlete-local windows, and the five required sections in order. The public workflow must retain source/tool + athlete-local window + coverage/freshness evidence; athlete-stated availability/duration separate from evidence; confirmed-race versus scenario-anchor wording; current-day wellness as partial; and the hard-session, baseline, and projection evidence limits. It must explicitly say it cannot derive an age rule, medical/injury conclusion, readiness/risk score, or automatic calendar edit—even after approval—and that gaps suppress only the affected conclusion and produce a focused question.

2. **Define three self-contained eval records, not just their themes.** Add named `CB-MASTERS-*` scenarios with `recipe: "masters-plan-review"` after adding that exact page. `run_eval.py` sends only each scenario’s `prompt`, so each prompt must state the athlete-local/source-labelled route and required output rather than relying on the MCP prompt or pack being invoked. For a well-instrumented scenario, require the profile/date anchor, bounded plan/history/race reads and applicable baseline/load/projection/wellness analyzers, five-section source separation, evidence-qualified hard-session spacing, explicit projection assumptions/targets, and only conditional proposals. For stale/missing wellness, require the freshness read, an explicit affected-dimension caveat/no invented readiness score, and one focused question. For the universal-age-rule request, require refusal of the requested cutoff/automatic spacing or ramp policy and a reframe to athlete-specific evidence without using age, medical, or injury-risk claims. Each record needs real `expected_tools`, all registered write/delete tools in `forbidden_tools` (plus raw-stream/heavy-payload routes as appropriate), and `must_address`/`anti_patterns` that reject chat-side calculations, title/load/calendar-proximity hard-session classification, missing-data-as-complete, age-derived policy, opaque scores, and writes. `--validate` checks only JSON shape, tool names, and page existence, so these semantic assertions must be encoded in the scenarios themselves.

3. **Make the discovery and roadmap edits exhaustive and registry-backed.** The registry now has thirteen prompts, while the cookbook index and PRD still say twelve. Plan the new six-argument `masters_plan_review` row and its strictly read-only/evidence-limited route in `web/content/reference/resources-prompts.md`, including the prompt-guardrail prose; update every public count/list in `_index.md` and the PRD in registry order; add an Unreleased entry covering the prompt, canonical pack, recipe, and evals; and amend v2.2 to state that its future cited/versioned rule engine remains distinct from this shipped no-rule evidence review. The portable-pack index (`docs/prompts/README.md`) and root README’s enumerated pack list are also stale once the pack is published; expand scope to update them. Explicitly review and record the disposition for the requested `season-and-block-plan.md` cross-link (and any other Check If Affected page), rather than leaving that decision implicit.

4. **Schedule documentation validation in addition to the named targeted checks.** After adding the page/card/links, run `make web-build` (at Step 3 or the existing verification step) so Hugo front matter and `relref` links are checked, alongside `go test ./internal/prompts` and `python3 scripts/eval/run_eval.py --validate`.

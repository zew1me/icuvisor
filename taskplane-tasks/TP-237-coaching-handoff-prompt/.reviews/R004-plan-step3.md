# Plan Review — TP-237 Step 3

## Verdict: REVISE

The Step 3 headings name the right surfaces, but they do not yet make the published workflow or eval enforce the already-approved handoff contract. Revise the plan to specify the following.

1. **Publish one contract consistently.** The cookbook page and existing Step 1 pack must describe both entry points (invoke `coaching_handoff` when MCP prompts are exposed, otherwise paste the pack), the manual review/copy/fresh-chat sequence, and the six ordered sections. They must preserve the distinction between user-explicit Goals/Constraints/Accepted decisions and tool-sourced evidence/current plan state; evidence rows need the claim, source tool, athlete-local date/window, and returned freshness/as-of. State `not provided` rather than inventing a timestamp, and retain the existing exclusions for credentials, raw IDs, paths, tokens, raw payloads/streams, sensitive health/location/free text, and unsupported physiological conclusions. Do not recreate a second divergent pack: the pack already exists from Step 1; publish/index and link that canonical content.

2. **Make the eval a real self-contained handoff test.** Add a named `CB-HANDOFF-*` scenario with `recipe: "conversation-handoff"` after creating that exact cookbook page. `run_eval.py` sends only a scenario's `prompt` to the agent; `recipe` merely validates that a page exists. Therefore the scenario prompt itself must supply explicit user-stated goals/constraints/accepted decisions and require the sectioned handoff, source labels, athlete-local date anchor, freshness (`as_of` or `not provided`), gaps/unresolved questions, and manual review/copy. Require `get_athlete_profile` and `resolve_calendar_dates`, plus the bounded plan/recent-evidence reads that the stated scenario needs; list the registered write/delete tools and `get_activity_streams` as forbidden. Its `must_address`/`anti_patterns` must explicitly reject merged conversation/tool sources, fabricated freshness or missing values, `include_full`/raw history, chat-side substitute calculations, unsupported physiology, and any credential/raw-athlete-ID/local-path leakage. Do not put a real identifier or secret in the committed scenario.

3. **Update every discovery surface against the registry, not by incrementing stale prose.** Add the pack to `docs/prompts/README.md`, the recipe card to the cookbook index, a precise row in `resources-prompts.md` (arguments/defaults/bounds, resources, read-only route, and output/privacy behavior), and a handoff-specific Claude Project block or fresh-chat instruction. Reconcile the index's current “nine” count/list and the PRD's older prompt list with `internal/prompts/registry.go`: the registry now has eleven prompts, while both prose lists also omit `ride_analysis`. The final public lists must either enumerate the actual registered catalog or be reworded so they do not claim an incorrect count. Assess the README's enumerated downloadable-pack sentence as part of the documented “Check If Affected” step and record the disposition.

4. **Finish the release/documentation edits explicitly.** Add `coaching_handoff` to the PRD prompt catalog and a concise `[Unreleased]` Added entry covering the read-only prompt and portable handoff pack. Use valid cookbook front matter and internal `relref` links, then run the planned prompt tests and eval validation after the scenario/page are in place.
